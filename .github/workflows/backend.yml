name: Backend CI

on:
  push:
    paths:
      - "apps/backend/**"
  pull_request:
    paths:
      - "apps/backend/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: |
          cd apps/backend
          npm install

      - name: Build project
        run: |
          cd apps/backend
          npm run build

      - name: Run tests
        run: |
          cd apps/backend
          npm test
